### 📋 Полный список задач (Roadmap) Этап 1: Подготовка и Проектирование
1. Анализ конкурентов : Изучить топ-10 сайтов по запросу "3d панели", выписать их плюсы (удобный каталог) и минусы.
2. Структура сайта (Sitemap) :
   - Главная (Витрина хитов).
   - Каталог (с разбивкой: гипс, дерево, МДФ, мягкие панели).
   - Карточка товара.
   - Страница "Монтаж" (очень важно для этой ниши).
   - Портфолио/Галерея в интерьере.
   - Контакты и Оплата/Доставка.
3. Выбор стека технологий :
   - Рекомендация : Next.js (React) —"use client";

import React, { createContext, useContext, useState, useEffect } from 'react';
import { Product } from '@/types';

export interface CartItem extends Product {
  quantity: number;
}

interface CartContextType {
  items: CartItem[];
  addToCart: (product: Product, quantity?: number) => void;
  removeFromCart: (productId: string) => void;
  updateQuantity: (productId: string, quantity: number) => void;
  clearCart: () => void;
  totalItems: number;
  totalPrice: number;
}

const CartContext = createContext<CartContextType | undefined>(undefined);

export function CartProvider({ children }: { children: React.ReactNode }) {
  const [items, setItems] = useState<CartItem[]>([]);
  const [isLoaded, setIsLoaded] = useState(false);

  // Load cart from localStorage on mount
  useEffect(() => {
    const savedCart = localStorage.getItem('cart');
    if (savedCart) {
      try {
        setItems(JSON.parse(savedCart));
      } catch (e) {
        console.error("Failed to parse cart from local storage", e);
      }
    }
    setIsLoaded(true);
  }, []);

  // Save cart to localStorage whenever it changes
  useEffect(() => {
    if (isLoaded) {
      localStorage.setItem('cart', JSON.stringify(items));
    }
  }, [items, isLoaded]);

  const addToCart = (product: Product, quantity: number = 1) => {
    setItems(currentItems => {
      const existingItem = currentItems.find(item => item.id === product.id);
      if (existingItem) {
        return currentItems.map(item =>
          item.id === product.id
            ? { ...item, quantity: item.quantity + quantity }
            : item
        );
      }
      return [...currentItems, { ...product, quantity }];
    });
  };

  const removeFromCart = (productId: string) => {
    setItems(currentItems => currentItems.filter(item => item.id !== productId));
  };

  const updateQuantity = (productId: string, quantity: number) => {
    if (quantity < 1) {
      removeFromCart(productId);
      return;
    }
    setItems(currentItems =>
      currentItems.map(item =>
        item.id === productId ? { ...item, quantity } : item
      )
    );
  };

  const clearCart = () => {
    setItems([]);
  };

  const totalItems = items.reduce((total, item) => total + item.quantity, 0);
  const totalPrice = items.reduce((total, item) => total + (item.price * item.quantity), 0);

  return (
    <CartContext.Provider value={{ items, addToCart, removeFromCart, updateQuantity, clearCart, totalItems, totalPrice }}>
      {children}
    </CartContext.Provider>
  );
}

export function useCart() {
  const context = useContext(CartContext);
  if (context === undefined) {
    throw new Error('useCart must be used within a CartProvider');
  }
  return context;
} идеально для SEO и быстродействия, плюс Tailwind CSS для стилей. Для админки можно взять Strapi или Sanity , либо готовое решение (Shopify/WooCommerce), если нужен быстрый старт без глубокой кастомизации. Этап 2: Дизайн (UI/UX)
4. Разработка прототипов : Схематичное расположение блоков.
5. Дизайн-концепция : Акцент на крупные качественные фото текстур. Минимализм, чтобы не отвлекать от рельефа панелей.
6. Мобильная адаптация : 70% трафика будет с телефонов (Instagram/Pinterest). Этап 3: Разработка (Frontend & Backend)
7. Настройка окружения : Инициализация репозитория, настройка CI/CD.
8. Разработка Каталога :
   - Фильтры: по материалу, по рисунку (волна, геометрия), по стилю (лофт, классика).
9. Карточка товара (Критически важно) :
   - Галерея с зумом текстуры.
   - Калькулятор расчета : Пользователь вводит ширину и высоту стены -> сайт считает кол-во панелей + клей + затирку.
10. Корзина и Чекаут : Интеграция платежных систем и выбор доставки (СДЭК, Деловые линии — панели тяжелые и хрупкие).
11. Страница "Монтаж" : Блок с инструкциями и видео. Этап 4: Контент и SEO
12. Фотосессия : Рендеры панелей в интерьере (Mockups) и реальные фото текстур.
13. SEO-оптимизация : Мета-теги для каждой модели (напр., "Гипсовая 3d панель Волна купить").
14. Наполнение блога : Статьи "Как клеить", "Как красить", "Свет для 3d панелей".
### 💡 Советы специально для ниши 3D панелей
1. Умный калькулятор — это must-have .
   Люди не знают, сколько штук им нужно. Сделайте так: клиент вводит размеры стены (например, 3x2.5м), а сайт сам считает: "Вам нужно 24 панели (из них 2 запасные на подрезку)". Это повышает конверсию в разы.
2. Продавайте "Комплектом" .
   В корзине предлагайте сопутствующие товары: клей для гипса, грунтовку, финишную краску. Это увеличивает средний чек на 20-30%.
3. Визуализатор (Примерочная) .
   Если бюджет позволяет, сделайте простую функцию: загрузить фото своей комнаты и наложить текстуру панели. Или хотя бы качественные рендеры "До/После".
4. Акцент на упаковку и гарантию целостности .
   Главный страх клиента — "приедут битые". Напишите крупно: "Страхуем груз, тройная упаковка, замена боя за наш счет".
5. Инструкции для мастеров .
   Часто панели покупают дизайнеры или прорабы. Сделайте раздел "Для дизайнеров" с 3D-моделями (obj/max) для скачивания, чтобы они могли вставить ваши панели в свои дизайн-проекты.